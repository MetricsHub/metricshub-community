connector:
  detection:
    appliesTo: [ Linux ]

beforeAll:
  testCompute:
    type: http
    url: /testUrl/

    computes:
      - type: awk
        script: scriptTest
        exclude:  excludeTest
        keep: keepTest
        variables:
          FS: ";"
          someConstant: "365.25"
          aTable: ${source::beforeAll.otherSource}